<%= form_for(@activity_skill) do |f| %>
  <% if @activity_skill.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@activity_skill.errors.count, "error") %> prohibited this activity_skill from being saved:</h2>

      <ul>
      <% @activity_skill.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :program_id %><br />
    <%= f.number_field :program_id %>
  </div>
  <div class="field">
    <%= f.label :skill_id %><br />
    <%= f.number_field :skill_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
